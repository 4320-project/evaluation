void safe_concat(char *dst, size_t dst_sz, const char *a, const char *b) { size_t a_len = strlen(a), b_len = strlen(b); if (a_len + b_len + 1 > dst_sz) return; memcpy(dst, a, a_len); memcpy(dst + a_len, b, b_len + 1); }